---
galaxy_info:
  author: Stan Bogatkin
  description: Ansible role to install syslog server and clients
  company: Personal project
  role_name: syslog

  license: Apache

  min_ansible_version: 2.4

  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal

  galaxy_tags: [openssl, logging, log, logs, syslog, syslog_ng, rsyslog]

dependencies:
  - sorrowless.ca
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
